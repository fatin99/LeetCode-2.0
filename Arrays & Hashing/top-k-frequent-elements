import heapq
from collections import Counter

class Solution:
    # O(nlogn): count the frequency of each number and then sort the array 
    # based on each element’s frequency. After that, select the top k
    # Somehow this performs faster??? 
    def topKFrequent(self, nums: List[int], k: int) -> List[int]:
        numCount = Counter(nums)
        sortedNumCount = sorted(numCount.items(), key=lambda item: item[1], reverse=True)
        topK = []
        i = 0
        while k > 0:
            topK.append(sortedNumCount[i][0])
            i += 1
            k -= 1
        return topK
    
    #Bucket Sort (O(n))
    def topKFrequent(self, nums: List[int], k: int) -> List[int]:
        bucket = [[] for _ in range(len(nums) + 1)]
        numCount = Counter(nums)
        for num, count in numCount.items():
            bucket[count].append(num)
        result = []
        for i in range(len(bucket)):
            for j in bucket[len(bucket) - i - 1]:
                result.append(j)
                if len(result) == k:
                    return result
        return result

# print(Solution().topKFrequent([1,2,1,2,1,2,3,1,3,2], 2))
print(Solution().topKFrequent([1,1,1,2,2,3], 2))

